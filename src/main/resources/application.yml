spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysqldb:3306/${DB_NAME}?useSSL=false
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    
    hikari:
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update


    # url: jdbc:mysql://localhost:3306/database?useSSL=false
    # username: root
    # password: password

  #   spring:
  # datasource:
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  #   url: jdbc:mysql://mysql/${DB_NAME}?useSSL=false
  #   username: ${DB_USERNAME}
  #   password: ${DB_PASSWORD}

  #   hikari:
  #     initialization-fail-timeout: 0

  # jpa:
  #   database-platform: org.hibernate.dialect.MySQL5Dialect
  #   generate-ddl: true
  #   show-sql: true
  #   hibernate:
  #     ddl-auto: update
